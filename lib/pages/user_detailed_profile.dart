import 'package:flutter/material.dart';

class UserDetailedProfile extends StatelessWidget {
  const UserDetailedProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
